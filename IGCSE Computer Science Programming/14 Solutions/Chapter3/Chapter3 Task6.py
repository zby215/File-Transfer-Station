speed = int(input('Enter the average speed: '))
distance = int(input('Enter the distance: '))

time = (distance / speed) * 60

print('The journey duration is', time, 'minutes')

input('\n\nPress RETURN to finish.')
